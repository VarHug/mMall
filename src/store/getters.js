export const cartList = state => state.cartList

export const addressList = state => state.addressList

export const checkedList = state => state.checkedList

export const checkedAddress = state => state.checkedAddress
